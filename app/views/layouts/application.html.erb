<!DOCTYPE html>
<html class="antialiased">
  <%= render "shared/head" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>


  <header class="w-full fixed">
    <%= render "shared/nav" %>
    <%= render "shared/flash_notice" %>
  </header>

  <!-- dashboard design -->
  <body class="flex flex-wrap bg-gray-100 w-full h-screen">

    <!-- only if logined in, show header -->
    <div class="w-64 bg-white rounded p-3 shadow-lg fixed top-0 bottom-0 mt-16">
      
      <%= render "shared/header" %>
    </div>

    <main class="pl-64 flex-1 mt-16">
      <%#= render "shared/flash_notice" %>
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>

  <!-- ** render must use # to comments out, original one -->
  <!--body>
    <%#= yield %> 
    <%#= render "shared/header" %>
    
    <main class="px-4 lg:px-10">
      <%#= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
  <%#= content_for?(:content) ? yield(:content) : yield %>

  <%#= yield %>

</html>
